version: '3'

tasks:
  lint:
    cmds:
      - golangci-lint run ./...

  fmt:
    cmds:
      - golangci-lint fmt ./...

  test:
    cmds:
      - go test -v -shuffle=on -race ./...

  test-integration:
    cmds:
      - go test -v -tags 'linux integration' ./internal/linux/ -run TestTPM
